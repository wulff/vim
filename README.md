Clone my repository into your homedir

    git clone https://github.com/ramlev/vim ~/.vim

Symlink the vimrc file from the repository to your homedir

    ln -s ~/.vim/vimrc ~/.vimrc

And you should up and running.
